version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 16
  pre_build:
    commands:
      - yarn
  build:
    commands:
      - yarn build

artifacts:
  files:
    - '**/*'
  base-directory: 'dist'
